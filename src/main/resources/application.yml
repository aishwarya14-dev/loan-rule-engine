spring:
  datasource:
    url: jdbc:postgresql://ep-floral-flower-aog3dqhi.c-2.ap-southeast-1.aws.neon.tech/neondb?sslmode=require
    username: neondb_owner
    password: npg_iZ0dV7vUrocm
#    driver-class-name: org.postgresql.Driver

    hikari:
      maximum-pool-size: 5
      minimum-idle: 1
      idle-timeout: 30000
      max-lifetime: 600000

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect

  flyway:
    fail-on-unsupported-database: false

  logging:
    level:
      root: INFO
      org.hibernate.SQL: DEBUG
      security: DEBUG

      org:
        springframework:
          web : DEBUG


server:
  servlet:
    context-path: /loan-processing
    session:
      timeout: 30m
      cookie:
        http-only: true
        secure: true
        same-site: Strict