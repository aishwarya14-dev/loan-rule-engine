apiVersion: 1

datasources:
  - name: Prometheus
    uid: prometheus-uid
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: true          # add this — required in Grafana 10+
    jsonData:
      timeInterval: '15s'
      httpMethod: POST


